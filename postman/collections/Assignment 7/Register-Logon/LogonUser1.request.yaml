$kind: http-request
name: LogonUser1
url: "{{HOST}}/api/users/logon"
method: POST
body:
  type: json
  content: |
    {
      "email": {{userBuddyEmail}},
      "password": "Pa$$word20"
    }
scripts:
  - type: afterResponse
    code: |
      pm.test("Status code is 200", function () {
          pm.response.to.have.status(200);
      });

      pm.test("Returns the expected name", function () {
          const jsonData = pm.response.json();
          pm.expect(jsonData.name).to.eql("Buddy");
      });
    language: text/javascript
order: 4348027013654338
