$kind: http-request
name: SearchTooShort
url: "{{HOST}}/api/analytics/tasks/search?q=a"
method: GET
queryParams:
  q: a
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 400 for short queries", function () {
          pm.response.to.have.status(400);
      });

      //only for singular test
      // pm.sendRequest({
      //     url: "{{HOST}}/api/users/logoff",
      //     method: 'DELETE',
      // }, (err, res) => {
      //     if (err) {
      //         console.log("Post-res logoff: ", err);
      //     } else {
      //         console.log("Server global variable is null");
      //     }
      // });
    language: text/javascript
  - type: beforeRequest
    code: |
      // this is only needed when we run singular tests

      // pm.sendRequest({
      //     url: "{{HOST}}/api/users/logon",
      //     method: 'POST',
      //     header: { 'Content-Type': 'application/json' },
      //     body: {
      //         mode: 'raw',
      //         raw: JSON.stringify({
      //             email: pm.globals.get("userBuddyEmail"),
      //             password: pm.globals.get("password")
      //         })
      //     }
      // }, function (err, res) {
      //     if (err) {
      //         console.log("Pre-request login failed: ", err);
      //     } else {
      //         console.log("Server global variable updated for user1");
      //     }
      // });
    language: text/javascript
order: 4150752982420127
