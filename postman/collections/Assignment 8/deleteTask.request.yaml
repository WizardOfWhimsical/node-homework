$kind: http-request
name: deleteTask
url: "{{HOST}}/api/tasks/{{todo_id}}"
method: DELETE
headers:
  X-CSRF-TOKEN: "{{csrfToken}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Delete success code is 200 OK", function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 4338533195994695
