1) SELECT SUM(products.price * line_items.quantity) AS total_price, orders.order_id FROM orders LEFT JOIN line_items ON orders.order_id = line_items.order_id LEFT JOIN products ON line_items.product_id = products.product_id GROUP BY orders.order_id ORDER BY orders.order_id;

3)-- BEGIN;
-- INSERT INTO orders(employee_id, customer_id, date) VALUES((SELECT employee_id FROM employees WHERE first_name = 'Miranda' AND last_name = 'Harris'),(SELECT customer_id FROM customers WHERE customer_name = 'Perez and Sons'), CURRENT_DATE) RETURNING order_id;

3)-- INSERT INTO line_items(order_id, product_id, quantity) VALUES( 258, (SELECT product_id FROM products ORDER BY price ASC LIMIT 5),10);
-- INSERT INTO line_items(order_id, product_id, quantity) SELECT 257, product_id, 10 FROM products ORDER BY price ASC LIMIT 5;

4) SELECT CONCAT(last_name, ' ', first_name) AS full_name, COUNT(orders.order_id) AS order_count FROM employees JOIN orders ON employees.employee_id = orders.employee_id GROUP BY employees.employee_id HAVING COUNT(orders.order_id) > 5 ORDER BY full_name ASC;
-- Test to make sure i was right
-- SELECT last_name, orders.order_id FROM employees JOIN orders ON employees.employee_id = orders.employee_id WHERE last_name = 'Bowman';

